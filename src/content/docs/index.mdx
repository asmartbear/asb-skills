---
title: Skills from A Smart Bear
description: Claude Code skills that implement frameworks and algorithms from A Smart Bear and the Hidden Multipliers book.
template: splash
hero:
  tagline: 'AI skills that facilitate you executing the frameworks and workflows from the <a href="https://longform.asmartbear.com" target="_blank" rel="noopener">A Smart Bear</a> blog and in the book <em><a href="https://hiddenmultipliers.com" target="_blank" rel="noopener">Hidden Multipliers</a></em>.'
---

import { LinkButton } from '@astrojs/starlight/components';
import SkillsList from '../../components/SkillsList.astro';
import WorkshopList from '../../components/WorkshopList.astro';
import CuratorByline from '../../components/CuratorByline.astro';

<CuratorByline />

<div class="hero-actions">
  <LinkButton href="/asb-skills.zip" icon="download" variant="primary" download>Download all skills</LinkButton>
  <LinkButton href="/install/" icon="right-arrow" variant="secondary">How to install</LinkButton>
  <LinkButton href="https://longform.asmartbear.com" icon="external" variant="secondary" target="_blank" rel="noopener"><span class="btn-stack"><span class="btn-eyebrow">The Blog</span>A Smart Bear</span></LinkButton>
  <LinkButton href="https://hiddenmultipliers.com" icon="external" variant="secondary" target="_blank" rel="noopener"><span class="btn-stack"><span class="btn-eyebrow">The Book</span>Hidden Multipliers</span></LinkButton>
</div>

<aside class="differentiator">
  <p class="differentiator-eyebrow">What makes these different</p>
  <p class="differentiator-body"><strong>They don't think for you</strong> or hand you an answer. Each one <strong>interrogates you</strong> — pressing on vague claims and refusing to let you off the hook for a weak answer — the way Jason would if he were facilitating in the room. You leave with your own best thinking, not the AI's.</p>
</aside>

<style is:global>{`
  /* Splash hero leaves a lot of vertical space; tighten it on this page. */
  .main-pane > .content-panel:first-child,
  .main-pane main { padding-block-start: 1rem !important; }
  .hero {
    padding-block-start: 0 !important;
    padding-block-end: 0.5rem !important;
  }
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    align-content: flex-start;
    row-gap: 0.5rem;
    column-gap: 0.75rem;
    margin-block: 0 2rem;
  }
  /* Defeat any default margin on Starlight's content-panel children so
     wrapped button rows don't pick up paragraph-style spacing. */
  .hero-actions > .sl-link-button {
    margin-block: 0 !important;
  }
  /* The Blog / The Book buttons carry a small uppercase label above their
     title so a first-time visitor knows what each destination is. */
  .hero-actions .btn-stack {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    line-height: 1.15;
    text-align: left;
  }
  .hero-actions .btn-eyebrow {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    opacity: 0.6;
  }

  /* "What makes these different" — an accented aside between the hero actions
     and the skills grid. Present enough to be read, but styled as an aside so
     it doesn't compete with the hero. */
  .differentiator {
    margin-block: 0 2rem;
    padding: 1rem 1.25rem;
    border-inline-start: 3px solid var(--sl-color-accent);
    background: var(--sl-color-accent-low);
    border-radius: 0.35rem;
    max-width: 46rem;
  }
  .differentiator-eyebrow {
    margin: 0 0 0.35rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--sl-color-accent-high);
  }
  .differentiator-body {
    margin: 0;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--sl-color-gray-1);
  }
  .differentiator-body strong {
    color: var(--sl-color-white);
    font-weight: 700;
  }

  /* Home page: Workshops and Skills side by side on wide screens; stacked
     (Workshops first, per source order) on narrow ones. */
  .home-columns {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    align-items: start;
  }
  @media (min-width: 50rem) {
    .home-columns {
      grid-template-columns: minmax(0, 22rem) minmax(0, 1fr);
      /* Three shared rows: title / blurb / list. Both columns span them via
         subgrid, so the titles align, the blurb tops align, and the first
         tiles align — regardless of how the blurbs wrap. */
      grid-template-rows: auto auto auto;
      column-gap: 3rem;
      row-gap: 0;
    }
    .home-col {
      display: grid;
      grid-row: 1 / span 3;
      grid-template-rows: subgrid;
      /* Critical: without this the list cell stretches to fill the tall
         shared list row (sized to the longer Skills column), ballooning the
         Workshops tiles. start keeps every cell — including the tiles — at
         its natural height, top-anchored in its row. */
      align-items: start;
    }
  }
  /* Starlight injects "margin-top:1rem" on any element that follows a sibling
     inside .sl-markdown-content (specificity 0,1,2) — it lands on the second
     column (dropping Skills below Workshops) and on every 2nd+ child inside a
     column. These 0,2,0 selectors outrank it and zero those offsets, so all
     vertical spacing comes solely from the explicit bottom margins below and
     every row lines up across the two columns. */
  .home-columns > .home-col,
  .home-columns .home-col > * {
    margin-top: 0;
  }
  .home-col-title {
    margin-block: 0 0.15rem;
    line-height: 1.15;
  }
  .home-col-blurb {
    margin-block: 0 0.85rem;
    color: var(--sl-color-gray-3);
    font-size: 0.95rem;
  }
`}</style>

<div class="home-columns">
  <section class="home-col home-col--workshops">
    <h2 class="home-col-title">Workshops</h2>
    <p class="home-col-blurb">Multi-step methods — sequences of skills, each completing a meaningful step.</p>
    <WorkshopList />
  </section>
  <section class="home-col home-col--skills">
    <h2 class="home-col-title">Standalone</h2>
    <p class="home-col-blurb">Single skills that each work on their own.</p>
    <SkillsList independentOnly />
  </section>
</div>
