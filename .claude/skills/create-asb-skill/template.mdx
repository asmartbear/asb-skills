---
title: TODO — hook + descriptive (filled at Phase 0). Full per-page title. Pulls the reader in ("tell me more") while still telling them what the skill is. Pithy, emotionally interesting, but never vague.
cardTitle: TODO — short title shown on the home-page card. 1–3 words. Falls back to `title` if omitted, but a long full title looks bad on the card. E.g. title="Rude Q&A: The Constructive Devil's Advocate" → cardTitle="Rude Q&A".
hook: TODO — verb-led one-liner shown on the home-page card. 5–8 words. Falls back to `summary` if omitted. Must scan in one beat. E.g. "Grills your plan until it cracks."
summary: TODO — benefit-first one-liner, ~150 chars, rendered as italic line at the top of the per-skill page and used for `<meta description>`. Lead with what the reader GETS (the outcome, the problem solved), then briefly say HOW. Not "This is a devil's-advocate interrogator" but "Stress-test your ideas to a fine point with a devil's-advocate interrogator." Broad at first, sharpened in later phases.
input: TODO — one-line description of what the user brings TO the skill (a plan, a draft, a brief, a number, etc.). Filled in Phase 3 once the interaction shape is clear. Renders as the top row of the Input/Output tile on the per-skill page. Both `input` AND `output` must be set for the tile to render.
output: TODO — one-line description of what the user GETS BACK from the skill (a markdown doc, a diagnosis, a list, etc.). Filled in Phase 3. Renders as the bottom row of the I/O tile.
# related: [asb-foo, asb-bar]  # Uncomment and list other public-skill names that someone using this skill would naturally reach for next. Renders as a "Related skills" section at the bottom of the per-page (and adds an entry to the right-side TOC). Skip / leave commented if there are no obvious follow-ons. One-directional — list only the skills useful as a follow-on from THIS one. Lint will error on unknown names.
source:
  # Primary-source citation. Renders as a short "Adapted from <title>." sentence appended to the italic summary at the top of the per-page. Use the foundation article most directly behind the skill — usually the first entry under `## From the source` Foundation. Skip the whole `source:` block to suppress the citation.
  title: "TODO — title of the primary A Smart Bear article (or chapter), as it appears in the article"
  url: "https://longform.asmartbear.com/SLUG/"
---

## What this is about

TODO — long description. This body renders as the public-facing page on the
website. The site shows: title (from frontmatter), italic summary, the
Installing box, then this body (starting with this `## What this is about`
heading). The actual SKILL.md content is NOT shown on the page — readers
get it via the Installing box (download / view / copy). So this prose is
what the reader actually sees and decides on.

By Phase 6 the 2–4 inviting paragraphs under `## What this is about` should:
(a) **Open with the reader's payoff** — the outcome, the change, the
    problem solved. Make them want to keep reading.
(b) Explain what the framework is and when it applies (the how).
(c) Set up the concrete example below — but don't pre-explain it, let the
    Example invocation section do that work.

## Example invocation

TODO — concrete invocation + brief description. Added in Phase 3 once the
interaction shape is clear. Use **second-person voice** throughout ("you,"
not "the user").

Structure: a lead-in sentence — *"You can invoke the skill like this:"* —
followed by a fenced code block (use ```text — the slash-command form is
not real code, so no syntax highlighting; we just want a clean monospace
box) with a realistic slash-command invocation including enough context
that the interrogation/diagnosis/etc. has something specific to bite on:

```text
/asb-<slug> <one or two sentences of your context — a real situation, with
enough specifics that the reader can imagine the conversation that follows>
```

Then **one or two sentences** saying roughly what happens — the shape of
the session and what the reader walks away with. Do NOT narrate the
step-by-step process; readers won't read that. Do NOT show literal LLM
output. Address the reader as "you."

## From the source

TODO — split into two parts: foundation (the 1–2 critical sources, called
out with a brief lead paragraph) and supporting (the rest). Each item gets
a one-line explanation of WHY/HOW it relates to the skill, not just a bare
link. Do NOT add "(A Smart Bear)" after each item — the URL makes the
source obvious.

Structure:

[One-paragraph lead identifying the 1–2 foundational articles, e.g. "Two
articles form the foundation of this skill:" — or "The primary article
behind this skill is …" if there's only one.]

- [**Article 1 title**](https://longform.asmartbear.com/SLUG/) — one-line explanation of why this is the critical source / what it contributes.
- [**Article 2 title**](https://longform.asmartbear.com/SLUG/) — one-line explanation of why this is also critical / its specific role.

[One short prose line introducing the supporting list, e.g. "Supporting
articles each cover a piece of the mechanism or the target the wielder is
reaching for:"]

- [**Supporting article**](https://longform.asmartbear.com/SLUG/) — what specific piece of the mechanism/target/concept this article supplies.
- [**Another supporting article**](https://longform.asmartbear.com/SLUG/) — same: one-line why-relevant.

For Hidden Multipliers chapters (no online URL per-chapter): italicize the
chapter name and reference the book by name with a single link to
<https://hiddenmultipliers.com>. Example:

- *Pricing power* (section "The whale curve") in *Hidden Multipliers* — <https://hiddenmultipliers.com> — one-line why-relevant.

If there are zero supporting items, just omit the "Supporting articles…"
paragraph and its list entirely. Don't leave an empty heading or section.
